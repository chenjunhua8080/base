<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjh.common.controller.excel.dao.EmployeeDao">

    <select id="getByLeaveTimeAndEmployeeId" resultType="com.cjh.common.controller.excel.po.EmployeeEntity">
        select * from employee
        <where>
            <if test="excelDate != null">
                AND (leave_time is null or (leave_time <![CDATA[>=]]> #{excelDate}))
            </if>

            and employee_id in
            <foreach item="item" index="index" collection="employeeIds" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>

</mapper>